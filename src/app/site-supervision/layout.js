export const metadata = {
  title: "Engineering Site Supervision",
  description: "On-site engineering supervision to ensure execution quality from project start to handover.",
};

export default function SiteSupervisionLayout({ children }) {
  return <>{children}</>;
}
