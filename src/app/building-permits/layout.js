export const metadata = {
  title: "Building Permits & Municipality Approvals",
  description: "We manage building permit applications and municipality approvals on behalf of our clients across Saudi Arabia.",
};

export default function BuildingPermitsLayout({ children }) {
  return <>{children}</>;
}
