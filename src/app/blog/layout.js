export const metadata = {
  title: "Blog",
  description: "Engineering insights, guides, and articles from MAN Engineering Consultancy.",
};

export default function BlogLayout({ children }) {
  return <>{children}</>;
}
